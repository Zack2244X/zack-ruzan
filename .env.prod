# Production environment for local Droplet deployment
IMAGE_NAME=quiz-platform:local
PORT=10000
# MySQL (local container)
MYSQL_ROOT_PASSWORD=ChangeMeRootPass!2026
DB_NAME=quiz_platform
DB_USER=qp_user
DB_PASSWORD=ChangeMeDBPass!2026
DB_HOST=db
DB_PORT=3306
# App
JWT_SECRET=$(openssl rand -base64 32 2>/dev/null || echo "change_this_secret")
ALLOWED_ORIGINS=http://localhost:3000
NEW_RELIC_LICENSE_KEY=
