# ============================================
#   Environment Variables — متغيرات البيئة
#   انسخ هذا الملف إلى .env واملأ القيم
# ============================================

# --- قاعدة البيانات (TiDB / MySQL) ---
DB_HOST=localhost
DB_PORT=4000
DB_NAME=quiz_platform
DB_USER=root
DB_PASSWORD=
DB_SSL=false

# --- المصادقة (Authentication) ---
JWT_SECRET=your-super-secret-jwt-key-change-this
JWT_EXPIRES_IN=2h
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# --- الأدمن (Admin) ---
ADMIN_EMAILS=admin@example.com,teacher@example.com
ADMIN_CREATE_SECRET=your-admin-creation-secret

# --- السيرفر (Server) ---
PORT=3000
NODE_ENV=development
ALLOWED_ORIGINS=http://localhost:3000,https://your-domain.com

# --- الحماية الموزعة (Distributed Security) ---
REDIS_URL=redis://127.0.0.1:6379
DDOS_FAIL_CLOSED=true

# --- صحة الخدمة (Health Endpoint Auth) ---
HEALTHCHECK_TOKEN=change-me-long-random-token
